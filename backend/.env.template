# ==========================================
# ETHIOPIAN IT PARK CMS - ENVIRONMENT CONFIGURATION
# ==========================================
# INSTRUCTIONS:
# 1. RENAME this file from ".env.template" to ".env"
# 2. Replace all REPLACE_WITH_* values with your actual credentials
# 3. DO NOT commit .env file to Git (it's already in .gitignore)

# ==========================================
# DATABASE CONNECTION MODE
# ==========================================
# Set to "true" for Plesk deployment (uses MYSQL* variables)
# Set to "false" for local development (uses DB_* variables)
USE_REMOTE_DB=true

# ==========================================
# REMOTE DATABASE CONFIG (For Plesk - when USE_REMOTE_DB=true)
# ==========================================
# Replace these values with credentials from Plesk after creating database
MYSQLHOST=localhost
MYSQLUSER=ethiop18_itpcms_user
MYSQLPASSWORD=REPLACE_WITH_THE_GENERATED_PASSWORD
MYSQLDATABASE=ethiop18_itpcms_db
MYSQLPORT=3306

# ==========================================
# LOCAL DATABASE CONFIG (For local development - when USE_REMOTE_DB=false)
# ==========================================
# These are for testing on your computer
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=cms
DB_PORT=3306

# ==========================================
# SECURITY SECRETS
# ==========================================
JWT_SECRET=hayaltamrat@27
SESSION_SECRET=hayaltamrat@27

# ==========================================
# EMAIL CONFIGURATION (Optional - for newsletters)
# ==========================================
# Get Gmail App Password from: Google Account → Security → App Passwords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=info@ethiopianitpark.et
EMAIL_PASS=REPLACE_WITH_GMAIL_APP_PASSWORD

# ==========================================
# FRONTEND URL
# ==========================================
# Change to your actual frontend URL
FRONTEND_URL=https://ethiopianitpark.et

# ==========================================
# SERVER PORT
# ==========================================
PORT=5005

# ==========================================
# DEPLOYMENT NOTES
# ==========================================
# 1. For Plesk deployment, set USE_REMOTE_DB=true
# 2. Create database in Plesk and update MYSQL* variables
# 3. Create a NEW .env file on server with Plesk credentials
# 4. Never upload this .env file to the server - create fresh one there!
